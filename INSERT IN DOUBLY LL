# Insert at end of Doubly Linked List
  input >>     1 <-> 2 <-> 3   x= 4   o/p 1 2 3  4
// as we know, it consists of 2 ptr next and prev ptr
// naive approach would be  
 curr = 


Node *curr= head;
Node *new;
if(head== null)
 head= new;
return;

while(curr->next!=null)
{
  curr= curr->next;
}
curr->next= new;
new->prev = curr;
