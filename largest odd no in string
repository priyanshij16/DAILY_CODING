1903. Largest Odd Number in String


Input: num = "52"
Output: "5"

 string largestOddNumber(string num) {
        int n= num.size()-1;
        for(int i=n;i>=0;i--){
            
            if ((num[i] -'0') %2)
             return num.substr(0,i+1);
        }
        return string;
