4. Median of Two Sorted Arrays

 // 1 2 3 4 >> size 4 median>2+3/2=2.5
    //1 2 3 >> o/p= 2
    vector<int>v;
    for(auto x: nums1)
    v.push_back(x);

    for(auto x: nums2)
    v.push_back(x);

    sort(v.begin(), v.end());
    int n= v.size();
     return n%2?v[n/2]:( v[n/2 -1] +v[n/2])/2.0;
