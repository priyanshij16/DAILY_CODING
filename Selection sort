 for (i = 0; i < n-1; i++) 
    { 
      min_idx = i;  // u can also use direct i variable :
        for (j = i+1; j < n; j++) 
        if (arr[j] < arr[min_idx]) 
            min_idx = j; 

        swap(&arr[min_idx], &arr[i]); 
    } 
