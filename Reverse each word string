// Reverse a String 
ex: Priya Jain
o/p: ayirp niaj

 void fuc(){
  stack<char> st;

for(int i=0;i<n;i++){
  if( s[i]!= " ")
{ 
 st.push(s[i);
}
   else{
   while( st.empty() == false)
    return st.top();
  st.pop();
   }
}
}
