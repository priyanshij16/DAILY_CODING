1 st APPROACH

  int rows = mat.size();'
int cols = mat[0].size();
for (int i=0;i<rows;i++){
  reverse(mat[i].begin(), mat[i].end());
    }

    return mat;

#####
2 nd approach 

start=0,end=n-1;
for(int i=0;i<n;i++)
{
swap(arr[i][start], arr[i][end])
start++;
end--;
}
